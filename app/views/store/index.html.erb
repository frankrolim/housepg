
<section class="jumbotron text-center">
  <div class="container">
    <h1 class="jumbotron-heading"> <%= t('head1')%></h1>
    <p class="lead text-muted"> <%= t('head2')%> </p>
    <p>
      <% if user_signed_in? %>
        Olá <strong><%= current_user.name %></strong>!
      <% else %>
        <%= link_to (t ('signup')), new_user_registration_path, class: "btn btn-primary" %>
        <%= link_to (t ('signin')), new_user_session_path, class: "btn btn-secondary" %>
      <% end %>
    </p>
  </div>
</section>


<div class="container">
  <h2><%= t ('events')%></h2>

  <%= render partial: "movies/movies", locals: {movies: @movies} %>
</div>
